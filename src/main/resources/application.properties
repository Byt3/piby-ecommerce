#security.ignored=/**
spring.http.multipart.max-file-size=15mb
spring.http.multipart.max-request-size=15mb
endpoints.health.sensitive=false
management.security.enabled=false
management.health.db.enabled=true 
management.health.defaults.enabled=true 
management.health.diskspace.enabled=true 

info.app.name=Piby Spring boot
info.app.provider=Marco Martini
info.contact.email=marcomartinidev@gmail.com

security.basic.enabled=true

#MySQL Connector section
spring.datasource.url= jdbc:mysql://localhost:3306/piby_ecommerce
spring.datasource.username=root
spring.datasource.password=729507Byt3Un1x

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect


spring.devtools.remote.secret=birthday-candle-tv-remove